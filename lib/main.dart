import 'package:flutter/material.dart';

void main() {
  runApp(const BasicSetup());
}


class BasicSetup extends StatelessWidget {
  const BasicSetup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
